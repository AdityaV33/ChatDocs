

ChatDocs is a lightweight RAG (Retrieval-Augmented Generation) web app that lets you upload a **PDF**, **DOCX**, or paste a **URL**, and then **chat with the content** using OpenAI + FAISS.

It supports conversational follow-up questions using chat history.

